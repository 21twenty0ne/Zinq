# Zinq — QR Code Generator

Современный генератор QR-кодов с поддержкой кастомизации, логотипов и экспорта в PNG/SVG.

![Zinq Preview](https://via.placeholder.com/800x450/0f172a/8b5cf6?text=Zinq+QR+Generator)

## ✨ Возможности

- **Полная кастомизация** — настройка цветов точек, углов и фона
- **Различные формы** — квадраты, круги, скругленные углы и другие стили
- **Логотипы** — загрузка своего изображения с настройкой размера и отступов
- **Экспорт** — скачивание в PNG и SVG форматах
- **Автоименование** — файлы автоматически получают имя на основе даты и контента
- **Сохранение настроек** — настройки сохраняются в localStorage
- **Адаптивный дизайн** — работает на десктопе и мобильных устройствах

## 🚀 Быстрый старт

### Требования

- Node.js 18+
- npm 9+

### Установка

```bash
# Клонируйте репозиторий
git clone https://github.com/your-username/zinq-qr-generator.git
cd zinq-qr-generator

# Установите зависимости
npm install

# Запустите dev-сервер
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Сборка для продакшена

```bash
npm run build
```

Готовые файлы будут в папке `dist/`

## 🛠️ Технологии

- **[React](https://react.dev)** — UI фреймворк
- **[Vite](https://vite.dev)** — сборщик
- **[TypeScript](https://www.typescriptlang.org)** — типизация
- **[Tailwind CSS](https://tailwindcss.com)** — стилизация
- **[qr-code-styling](https://github.com/AlyxMoon/qr-code-styling)** — генерация QR-кодов
- **[Lucide React](https://lucide.dev)** — иконки

## 📁 Структура проекта

```
src/
├── components/
│   ├── settings/          # Панели настроек
│   │   ├── BackgroundSettings.tsx
│   │   ├── ContentSettings.tsx
│   │   ├── CornersSettings.tsx
│   │   ├── DotsSettings.tsx
│   │   ├── LogoSettings.tsx
│   │   └── SettingsPanel.tsx
│   ├── ui/               # UI компоненты
│   │   ├── Accordion.tsx
│   │   ├── ColorPicker.tsx
│   │   ├── Select.tsx
│   │   └── Slider.tsx
│   ├── DownloadButton.tsx
│   └── QRCodePreview.tsx
├── hooks/
│   ├── useLocalStorage.ts
│   └── useQRCode.ts
├── types/
│   └── qr.ts            # TypeScript типы
├── App.tsx
├── index.css
└── main.tsx
```

## 🎨 Настройки QR-кода

### Контент
Введите URL, текст, номер телефона, email или любые другие данные для кодирования.

### Точки
- **Цвет** — выберите любой цвет
- **Форма** — квадрат, точки, скругленные, очень скругленные, классические

### Углы
- **Внешняя рамка** — цвет и форма
- **Центр угла** — цвет и форма (квадрат или точка)

### Фон
- **Цвет** — любой цвет
- **Прозрачность** — поддержка прозрачного фона (отображается шахматным паттерном)

### Логотип
- **Загрузка** — поддержка PNG, JPG, SVG
- **Размер** — от 10% до 50% от размера QR
- **Отступ** — настраиваемый отступ вокруг логотипа

## 📄 Лицензия

MIT License — свободное использование в личных и коммерческих проектах.

---

Создано с ❤️ и `qr-code-styling`
